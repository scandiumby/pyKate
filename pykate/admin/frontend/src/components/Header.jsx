import React from 'react';
import {MainMenu} from "@epam/promo";

const Header = () => {
    return (
        <header>
            <div>
                <MainMenu/>
            </div>
        </header>
    );
};

export default Header;